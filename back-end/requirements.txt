# --- Framework & extensions ---
Flask==3.1.2
Flask-Argon2==0.3.0.0
Flask-Bcrypt==1.0.1
flask-cors==6.0.2
Flask-Migrate==4.1.0
Flask-Script==0.4.0

# --- WSGI server ---
gunicorn==24.1.1

# --- DB drivers ---
mariadb==1.1.14        # MariaDB Connector/Python (nom du package = 'mariadb')
PyMySQL==1.1.2

# --- Auth / Tokens ---
PyJWT==2.10.1
msal==1.34.0

# --- Tests & outils ---
pytest==9.0.2
python-dotenv==1.2.1
virtualenv==20.36.1

# --- Crypto ---
pycryptodome==3.23.0

# --- Observabilité / OTEL ---
opentelemetry-sdk==1.39.1
opentelemetry-exporter-otlp==1.39.1

# --- Swagger UI bundle ---
# Le bundle qui embarque Swagger UI 5.21.0 côté Python est généralement 'swagger-ui-bundle'.
# Selon les build, swagger-ui-bundle==0.0.9 embarque une version 5.x récente de Swagger UI.
swagger-ui-bundle==0.0.9